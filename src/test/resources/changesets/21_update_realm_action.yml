id: update-realm
author: klg71
realm: integ-test
changes:
  - updateRealm:
      id: testAdd
      rememberMe: true
      ssoSessionIdleTimeout: 3600
      passwordPolicy:
        notUsername: "this value is not used"
        minLength: 8
